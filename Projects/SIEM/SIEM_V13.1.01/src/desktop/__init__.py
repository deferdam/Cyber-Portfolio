"""Desktop application (v13, PySide6).

A native shell over the existing Mini SOAR engine. The Qt-free EngineController holds the
logic; window.py is the Qt view; main.py is the entry point. PySide6 is a desktop-only
optional dependency (see requirements-desktop.txt), never required by the web build.
"""

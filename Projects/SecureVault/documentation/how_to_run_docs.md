# Documentation – How to build and view it locally

This project includes a simple documentation site powered by MkDocs. The site
is built from Markdown files and can be served on a local HTTP server that you
can open in your browser.

## 1. Install dependencies

From the project root:

```bash
python -m pip install -r requirements.txt
python -m pip install mkdocs